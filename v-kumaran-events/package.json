{
  "name": "v-kumaran-events",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html script.js styles.css dist/ && (cp MobileLandingPage.css dist/ || true) && (cp -r assets dist/ || true)"
  },
  "dependencies": {
    "cloudinary": "^1.44.0"
  }
}
